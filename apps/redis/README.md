# Redis

Deferred by default on this VM. If your application can avoid Redis initially, skip it.

If you must use Redis, prefer a small single-instance chart instead of KubeBlocks HA on this machine.
